# PawFlow / Veto-Care — Python Dependencies
# Install with:  pip install -r requirements.txt

Flask==3.0.3
supabase>=2.9.0
python-dotenv==1.0.1
Werkzeug==3.0.2
Flask-Cors==4.0.1

# Production WSGI server (used by Vercel / any cloud host)
gunicorn==22.0.0
